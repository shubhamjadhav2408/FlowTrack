VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-anon-key-here

# NOTE: The Supabase "publishable key" IS the anon key — use that one here.
# NEVER put your service_role (secret) key in this file — it must stay server-side only.
